<table id="datatable" class="table table-striped table-hover">
  <thead>
    <tr>
      <th>Action</th>
      <th>First name</th>
      <th>Middle name</th>
      <th>Last name</th>
      <th>Address</th>
      <th>Contact no</th>
      <th>Raid Title</th>
    </tr>
  </thead>

  <tbody>
    <% @employers.each do |employer| %>
      <tr>
        <td>
          <div>
          <%= link_to '#', class: "dropdown-toggle", :'data-toggle'=> "dropdown" do %><i class="fa fa-caret-right fa-2x"></i><%end%>
          <ul class="dropdown-menu dropdown-menu-action">
            <li><%= link_to 'Edit', edit_employer_path(employer) %></li>
            <li><%= link_to 'Destroy', employer, method: :delete, data: { confirm: 'Are you sure?' } %></li>
          </ul>
          </div>
        </td>
        <td><%= link_to employer.first_name, edit_employer_path(employer) %></td>
        <td><%= employer.middle_name %></td>
        <td><%= employer.last_name %></td>
        <td><%= employer.address %></td>
        <td><%= employer.contact_no %></td>
        <td><%= employer.raid_title %></td>
      </tr>
    <% end %>
  </tbody>
</table>