<h1>Edit User</h1>

<%= form_for(@user) do |f| %>
  <%= render 'form', f: f, user: @user%>
  <div class="field">
    <%= f.label :email %><br>
    <%= f.email_field :email, disabled: true%>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>


<%= link_to 'Back', users_path %>
